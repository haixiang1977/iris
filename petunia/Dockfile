FROM node:10.20.1

WORKDIR /home

COPY petunia /home/

RUN apt-get update
RUN apt-get install -y sqlite3 vim

WORKDIR /home/petunia
RUN npm install

# change redis/redis_client.js to point to redis docker container
# change mqtt/mqtt_sub.js to point to mosquitto docker container

RUN npm start
